import { useState } from 'react'; import { PageShell } from '../../components/common/PageShell'; import { school } from '../../data/school'; export function ContactPage(){const [sent,setSent]=useState(false);return <PageShell eyebrow="Contact" title="Talk to the British English School team."><div className="admission-layout"><form className="form-card" onSubmit={e=>{e.preventDefault();setSent(true)}}><h2>Leave a message</h2><label>Name<input required placeholder="Enter full name"/></label><label>Email<input required type="email" placeholder="Email id"/></label><label>Mobile<input required placeholder="Mobile no."/></label><label>Message<textarea rows={5}/></label><button className="btn btn-primary">Send message</button>{sent&&<p className="form-success">Message captured in this frontend demo.</p>}</form><aside className="content-card"><h3>Contact information</h3><p>{school.address}</p><p>{school.email}</p><p>{school.phones.join(', ')}</p><p>{school.hours}</p></aside></div></PageShell>}
