import { PageShell } from '../../components/common/PageShell'; import { toppers } from '../../data/toppers'; export function ToppersPage(){return <PageShell eyebrow="Achievement" title="Celebrating academic excellence."><div className="topper-grid page-toppers">{toppers.map(([name,cls,score],i)=><article key={name} className="topper-card"><span>0{i+1}</span><div className="avatar">{name.split(' ').map(x=>x[0]).join('')}</div><h3>{name}</h3><p>Class {cls}</p><strong>{score}</strong><small>2025-2026</small></article>)}</div></PageShell>}
