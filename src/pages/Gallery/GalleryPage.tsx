import { PageShell } from '../../components/common/PageShell'; import { galleryCategories } from '../../data/content'; export function GalleryPage(){return <PageShell eyebrow="Gallery" title="A glimpse of campus life and shared memories."><div className="gallery-mosaic full-gallery">{galleryCategories.map((x,i)=><button key={x} className={'gallery-tile tile-'+(i%8)}><span>{x}</span></button>)}</div></PageShell>}
