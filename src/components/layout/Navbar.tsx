import { NavLink, Link } from 'react-router-dom'; import { Menu, X, ChevronDown } from 'lucide-react'; import { useState } from 'react'; import { navigation } from '../../data/navigation'; export function Navbar(){const [open,setOpen]=useState(false);return <header className="navbar"><div className="container nav-inner"><Link to="/" className="brand"><img src="/assets/images/logo/bes-logo.svg" alt="British English School logo"/><span><b>BRITISH ENGLISH SCHOOL</b><small>Gere, Manpur, Gaya</small></span></Link><button className="menu-btn" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button><nav className={open?'nav-links open':'nav-links'}>{navigation.map(item=><NavLink key={item.to} to={item.to} onClick={()=>setOpen(false)}>{item.label}{item.children&&<ChevronDown size={15}/>}</NavLink>)}<Link className="nav-cta" to="/admission" onClick={()=>setOpen(false)}>Apply Now</Link></nav></div></header>}
